# IoT Sentinel Hub

Event-driven IoT device management dashboard with sensor monitoring, alerting, and device lifecycle management.

## Architecture
- `devices/` - Device registry and management
- `sensors/` - Sensor data collection and aggregation
- `alerts/` - Alert rules engine and notification dispatch
- `api/` - REST API endpoints
